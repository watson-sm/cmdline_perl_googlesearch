Just a silly utility that takes either a complete string, or if
searching with a sentence use a quoted string like “Hello World” as an
argument and then uses google.com to submit the form. Taking the replay and outputs to an html file.

